model User {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  role         String   @default("USER") // "ADMIN", etc.
  createdAt    DateTime @default(now())
}